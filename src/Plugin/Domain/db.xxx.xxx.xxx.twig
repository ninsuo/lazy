{% autoescape false %}
;
; BIND reverse data file for {{ arpa }}.XXX interface
;
$TTL	86400
@	IN	SOA	ns.{{ primary }}. {{ email }}. (
			{{ timestamp }}	; Serial
			 10800		; Refresh
			  3600		; Retry
			604800		; Expire
			 10800 )	; Negative Cache TTL
;
@	IN	NS	ns.{{ primary }}.
{% for domain in domains %}
{{ revArpa }}	IN	PTR	ns.{{ domain }}.
{% endfor %}

{% endautoescape %}
